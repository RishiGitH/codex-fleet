# Docker

This directory will contain local Plane and codex-fleet compose files.

Phase 1 keeps Docker as documentation only. Phase 2 will add:

- `docker-compose.plane.yml`
- health checks
- setup scripts
- environment wiring

We intentionally do not vendor Plane source code here.
